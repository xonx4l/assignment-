enum Shape {
    rectangle(f64, f64),
    square(f64),
    circle(f64),
}

fn calculate_area(shape: Shape) -> f64 {
    return 0;
}

fn main() {
    let rectangle = Shape::rectangle = (0.6, 0.8);
    let square = Shape::square = (0.5);
    let square = Shape::circle = (0.3);
}
